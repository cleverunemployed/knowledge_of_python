# Полное руководство по Git командам

## 📋 Содержание
1. [Основные команды](#основные)
2. [Работа с ветками](#ветки)
3. [История и просмотр](#история)
4. [Отмена изменений](#отмена)
5. [Работа с удаленным репозиторием](#удаленный)
6. [Слияние и конфликты](#слияние)
7. [Сохранение временных изменений](#временные)
8. [Продвинутые команды](#продвинутые)
9. [Конфигурация](#конфигурация)
10. [Полезные алиасы](#алиасы)

---

## 🔧 **1. Основные команды** <a name="основные"></a>

### **Инициализация репозитория**
```bash
# Создать новый репозиторий в текущей папке
git init
# Зачем: Превращает обычную папку в Git-репозиторий

# Клонировать удаленный репозиторий
git clone https://github.com/user/repo.git
git clone https://github.com/user/repo.git my-folder
# Зачем: Скачать весь проект с удаленного сервера (GitHub, GitLab и т.д.)
```

### **Просмотр состояния**
```bash
# Показать статус файлов
git status
# Зачем: Увидеть какие файлы изменены, добавлены или удалены

# Краткий статус
git status -s  # или --short
# Зачем: Компактный вывод (одна строка на файл)

# Показать изменения в файлах
git diff
# Зачем: Увидеть конкретные изменения в коде до коммита

# Показать изменения конкретного файла
git diff file.py
# Зачем: Увидеть изменения только в определенном файле

# Показать изменения в staged файлах
git diff --staged
git diff --cached  # то же самое
# Зачем: Увидеть изменения, которые уже добавлены в индекс
```

### **Добавление файлов в индекс (staging area)**
```bash
# Добавить все измененные файлы
git add .
# Зачем: Подготовить ВСЕ изменения к коммиту

# Добавить конкретный файл
git add file.py
# Зачем: Подготовить только определенный файл

# Добавить все файлы с расширением .py
git add *.py
# Зачем: Добавить только Python файлы

# Добавить все измененные файлы (кроме удаленных)
git add -u
# Зачем: Обновить индекс только для отслеживаемых файлов

# Интерактивное добавление
git add -p
# Зачем: Пошагово выбрать какие изменения добавлять (патчами)
# Полезно для разделения изменений на логические коммиты
```

### **Создание коммитов**
```bash
# Создать коммит с сообщением
git commit -m "Добавил новую функцию"
# Зачем: Сохранить изменения в истории репозитория

# Создать коммит с открытием редактора для сообщения
git commit
# Зачем: Написать развернутое сообщение коммита

# Добавить все изменения и создать коммит одной командой
git commit -a -m "Сообщение"
# Зачем: Быстрый коммит всех измененных отслеживаемых файлов
# ВНИМАНИЕ: Не добавляет новые (untracked) файлы!

# Создать коммит с подписью
git commit -S -m "Подписанный коммит"
# Зачем: Добавить GPG-подпись для верификации

# Исправить последний коммит
git commit --amend
# Зачем: Изменить сообщение или добавить файлы в последний коммит
# ВНИМАНИЕ: Изменяет историю!
```

### **Удаление и перемещение файлов**
```bash
# Удалить файл из индекса и рабочей директории
git rm file.txt
# Зачем: Удалить файл и запланировать это удаление для коммита

# Удалить файл только из индекса (оставить в рабочей директории)
git rm --cached file.txt
# Зачем: Перестать отслеживать файл (например, для .env файлов)

# Переименовать или переместить файл
git mv old.txt new.txt
# Зачем: Git понимает, что это перемещение, а не удаление+добавление

# Создать пустой коммит (без изменений)
git commit --allow-empty -m "Пустой коммит"
# Зачем: Запустить CI/CD или отметить важное событие
```

---

## 🌿 **2. Работа с ветками** <a name="ветки"></a>

```bash
# Показать все ветки
git branch
# Зачем: Увидеть список локальных веток (* отмечает текущую)

# Показать все ветки (включая удаленные)
git branch -a
# Зачем: Увидеть полную картину всех веток

# Создать новую ветку
git branch feature-new
# Зачем: Начать разработку новой функциональности

# Переключиться на ветку
git checkout feature-new
# Зачем: Перейти к работе в другой ветке

# Создать и переключиться на новую ветку
git checkout -b feature-new
# Зачем: Быстро начать работу в новой ветке

# Удалить ветку (если она слита)
git branch -d feature-new
# Зачем: Очистить список веток от завершенных

# Удалить ветку (принудительно)
git branch -D feature-new
# Зачем: Удалить ветку даже если она не слита

# Переименовать ветку
git branch -m old-name new-name
# Зачем: Изменить название ветки

# Создать ветку от определенного коммита
git branch new-branch abc1234
# Зачем: Начать с конкретного состояния проекта
```

---

## 📜 **3. История и просмотр** <a name="история"></a>

```bash
# Показать историю коммитов
git log
# Зачем: Увидеть последовательность изменений

# Краткая история
git log --oneline
# Зачем: Компактный вывод (один коммит - одна строка)

# Графическая история
git log --graph --oneline --all
# Зачем: Визуализировать ветвление и слияние

# История с изменениями в файлах
git log -p
# Зачем: Увидеть не только коммиты, но и изменения кода

# История конкретного файла
git log --follow file.py
# Зачем: Проследить историю файла, даже если он перемещался

# Кто последний менял строку
git blame file.py
# Зачем: Увидеть автора каждой строки кода

# Поиск по сообщениям коммитов
git log --grep="bug"
# Зачем: Найти коммиты по ключевым словам

# История за определенный период
git log --since="2024-01-01" --until="2024-12-31"
# Зачем: Отфильтровать коммиты по дате

# Показать изменения между ветками
git log master..feature
# Зачем: Увидеть что есть в feature, но нет в master

# Показать последний коммит
git log -1
# Зачем: Быстро посмотреть последнее изменение
```

---

## ↩️ **4. Отмена изменений** <a name="отмена"></a>

```bash
# Отменить изменения в рабочей директории
git checkout -- file.py
# Зачем: Вернуть файл к состоянию последнего коммита

# Отменить добавление файла в индекс
git reset HEAD file.py
# Зачем: Убрать файл из staged состояния

# Отменить последний коммит (сохранить изменения)
git reset --soft HEAD~1
# Зачем: Отменить коммит, но оставить изменения в индексе

# Отменить последний коммит (удалить изменения из индекса)
git reset HEAD~1
# Зачем: Отменить коммит, оставить изменения в рабочей директории

# Полностью удалить последний коммит
git reset --hard HEAD~1
# Зачем: Полностью отменить коммит и все изменения
# ВНИМАНИЕ: Изменения будут удалены безвозвратно!

# Отменить несколько коммитов
git reset --hard abc1234
# Зачем: Вернуться к определенному коммиту

# Откатить конкретный коммит
git revert abc1234
# Зачем: Создать новый коммит, отменяющий изменения указанного коммита
# Безопаснее чем reset, так как не перезаписывает историю

# Откатить последний коммит
git revert HEAD
# Зачем: Безопасно отменить последние изменения

# Очистить неотслеживаемые файлы
git clean -n  # показать что будет удалено
git clean -f  # удалить
git clean -fd # удалить файлы и директории
# Зачем: Удалить мусорные файлы (сгенерированные бинарники и т.д.)
```

---

## ☁️ **5. Работа с удаленным репозиторием** <a name="удаленный"></a>

```bash
# Показать удаленные репозитории
git remote -v
# Зачем: Увидеть все подключенные удаленные репозитории

# Добавить удаленный репозиторий
git remote add origin https://github.com/user/repo.git
# Зачем: Привязать локальный репозиторий к удаленному

# Изменить URL удаленного репозитория
git remote set-url origin https://new-url.git
# Зачем: Изменить адрес репозитория (при переносе)

# Удалить удаленный репозиторий
git remote remove origin
# Зачем: Отвязать репозиторий

# Загрузить изменения с удаленного репозитория
git fetch origin
# Зачем: Получить последние коммиты и ветки, но не сливать

# Загрузить и объединить изменения
git pull origin main
# Зачем: Получить изменения и сразу слить с текущей веткой

# Сокращенная форма (если upstream установлен)
git pull
# Зачем: Быстро обновить текущую ветку

# Слить с опцией rebase вместо merge
git pull --rebase
# Зачем: Сохранить линейную историю

# Отправить изменения на удаленный репозиторий
git push origin main
# Зачем: Отправить свои коммиты на сервер

# Сокращенная форма
git push
# Зачем: Отправить в upstream ветку

# Отправить ветку и установить upstream
git push -u origin feature
# Зачем: Отправить новую ветку и связать ее с удаленной

# Принудительная отправка (перезаписать историю)
git push --force
git push --force-with-lease  # безопаснее
# Зачем: Исправить историю после rebase/amend
# ВНИМАНИЕ: Может сломать работу других разработчиков!

# Удалить ветку на удаленном репозитории
git push origin --delete old-branch
# Зачем: Очистить удаленный репозиторий от ненужных веток

# Скачать все ветки
git fetch --all
# Зачем: Получить все изменения со всех удаленных репозиториев

# Показать информацию об удаленном репозитории
git remote show origin
# Зачем: Увидеть подробную информацию о связях веток
```

---

## 🔀 **6. Слияние и конфликты** <a name="слияние"></a>

```bash
# Слить другую ветку в текущую
git merge feature
# Зачем: Внести изменения из feature в текущую ветку

# Слить с сообщением
git merge feature -m "Слияние feature"
# Зачем: Добавить описание слияния

# Прервать слияние при конфликте
git merge --abort
# Зачем: Отменить слияние и вернуться к состоянию до него

# Показать конфликтующие файлы
git status
# Зачем: Увидеть какие файлы требуют ручного разрешения

# После разрешения конфликтов
git add resolved-file.py
git commit
# Зачем: Завершить слияние

# Слияние с опцией fast-forward
git merge --ff-only feature
# Зачем: Слить только если возможно без создания коммита слияния

# Показать конфликтующие части в файле
git diff --name-only --diff-filter=U
# Зачем: Быстро найти файлы с конфликтами
```

### **Rebase (перебазирование)**
```bash
# Перебазировать текущую ветку на master
git rebase master
# Зачем: "Переставить" коммиты поверх другой ветки

# Интерактивный rebase
git rebase -i HEAD~3
# Зачем: Изменить несколько последних коммитов (объединить, переименовать и т.д.)

# Продолжить rebase после разрешения конфликтов
git rebase --continue
# Зачем: Продолжить перебазирование после исправления конфликтов

# Прервать rebase
git rebase --abort
# Зачем: Отменить rebase и вернуться к исходному состоянию

# Пропустить коммит в rebase
git rebase --skip
# Зачем: Пропустить проблемный коммит (осторожно!)
```

---

## 📦 **7. Сохранение временных изменений** <a name="временные"></a>

```bash
# Сохранить незакоммиченные изменения
git stash
# Зачем: Временно убрать изменения, чтобы переключить ветку

# Сохранить с сообщением
git stash push -m "Работа над функцией"
# Зачем: Добавить описание к сохраненным изменениям

# Показать список сохраненных изменений
git stash list
# Зачем: Увидеть все stash'и

# Применить последние сохраненные изменения
git stash pop
# Зачем: Вернуть изменения и удалить их из stash

# Применить сохраненные изменения (не удаляя из stash)
git stash apply
# Зачем: Вернуть изменения, но оставить копию в stash

# Применить конкретный stash
git stash apply stash@{1}
# Зачем: Вернуть определенные сохраненные изменения

# Создать ветку из stash
git stash branch new-branch stash@{0}
# Зачем: Начать новую ветку с сохраненными изменениями

# Удалить stash
git stash drop stash@{0}
# Зачем: Очистить ненужные сохраненные изменения

# Очистить все stash'и
git stash clear
# Зачем: Полностью очистить временные сохранения
```

---

## 🚀 **8. Продвинутые команды** <a name="продвинутые"></a>

```bash
# Поиск по коду во всех коммитах
git log -p --all -S "function_name"
# Зачем: Найти когда и кем была добавлена/удалена строка

# Поиск по регулярному выражению
git log -p --all -G "regex_pattern"
# Зачем: Поиск по сложным шаблонам

# Показать изменения между коммитами
git diff abc123..def456
# Зачем: Увидеть разницу между двумя точками в истории

# Показать статистику изменений
git diff --stat master..feature
# Зачем: Увидеть сколько файлов и строк изменилось

# Показать дерево файлов
git ls-tree HEAD
# Зачем: Увидеть структуру файлов в репозитории

# Экспортировать репозиторий без истории
git archive --format=zip -o repo.zip HEAD
# Зачем: Создать архив с кодом для отправки

# Проверить целостность репозитория
git fsck
# Зачем: Найти проблемы с данными в репозитории

# Оптимизировать репозиторий
git gc
# Зачем: Сжать и очистить репозиторий для экономии места

# Показать текущий тег
git describe --tags
# Зачем: Увидеть ближайший тег (для версионирования)

# Создать архив текущего состояния
git bundle create repo.bundle HEAD
# Зачем: Создать один файл со всей историей (для передачи)
```

---

## ⚙️ **9. Конфигурация Git** <a name="конфигурация"></a>

```bash
# Установить имя пользователя
git config --global user.name "Ваше Имя"
# Зачем: Указывается в коммитах

# Установить email
git config --global user.email "ваш@email.com"
# Зачем: Связь с вашим аккаунтом

# Показать все настройки
git config --list
# Зачем: Увидеть текущую конфигурацию

# Установить редактор по умолчанию
git config --global core.editor "code --wait"
# Зачем: Выбрать свой любимый редактор для сообщений

# Установить цветной вывод
git config --global color.ui auto
# Зачем: Включить подсветку в терминале

# Установить алиасы (сокращения)
git config --global alias.co checkout
git config --global alias.br branch
git config --global alias.ci commit
git config --global alias.st status
# Зачем: Создать удобные сокращения команд

# Включить автодополнение
# Для bash: source /usr/share/bash-completion/completions/git
# Для zsh: autoload -Uz compinit && compinit
# Зачем: Удобное дополнение команд табом

# Игнорирование регистра в именах файлов
git config --global core.ignorecase false
# Зачем: Для проектов с разными ОС

# Установить размер кэша для улучшения производительности
git config --global core.preloadindex true
git config --global core.fscache true
# Зачем: Ускорить работу с большими репозиториями
```

---

## 🎯 **10. Полезные алиасы (добавить в ~/.gitconfig)** <a name="алиасы"></a>

```ini
[alias]
    # Основные сокращения
    co = checkout
    br = branch
    ci = commit
    st = status
    df = diff
    
    # Просмотр истории
    lg = log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit
    lol = log --graph --decorate --pretty=oneline --abbrev-commit
    lola = log --graph --decorate --pretty=oneline --abbrev-commit --all
    
    # Работа с ветками
    bclean = "!f() { git branch --merged ${1-master} | grep -v " ${1-master}$" | xargs -r git branch -d; }; f"
    bdone = "!f() { git checkout ${1-master} && git up && git bclean ${1-master}; }; f"
    
    # Упрощение команд
    up = pull --rebase
    save = stash
    pop = stash pop
    amend = commit --amend
    unstage = reset HEAD --
    last = log -1 HEAD
    
    # Полезные комбинации
    undo = reset HEAD~1
    wip = commit -am "WIP"
    wipe = !git add -A && git commit -qm 'WIPE SAVEPOINT' && git reset HEAD~1 --hard
    cleanup = "!git branch --merged | grep -v '*' | xargs -n 1 git branch -d"
```

---

## 🎓 **Полезные рабочие процессы**

### **Стандартный рабочий день:**
```bash
# Утром
git pull --rebase  # обновиться
git status         # проверить состояние

# В процессе работы
git add -p         # добавить изменения частями
git commit -m "..." # коммитить часто

# Перед отправкой
git pull --rebase  # обновиться снова
git push           # отправить изменения
```

### **При возникновении конфликта:**
```bash
# Способ 1: Через merge
git pull origin main
# Разрешить конфликты вручную
git add .
git commit -m "Resolve merge conflicts"

# Способ 2: Через rebase (предпочтительнее)
git pull --rebase origin main
# Разрешить конфликты
git add .
git rebase --continue
```

### **Экстренные ситуации:**
```bash
# Ой, сделал коммит не в ту ветку
git reset --soft HEAD~1  # отменить коммит, сохранив изменения
git stash                # сохранить изменения
git checkout correct-branch
git stash pop            # вернуть изменения
git add .
git commit -m "..."

# Ой, забыл добавить файл в последний коммит
git add forgotten-file.py
git commit --amend --no-edit  # добавить в предыдущий коммит

# Ой, написал неправильное сообщение коммита
git commit --amend -m "Новое правильное сообщение"
```

---

## 📚 **Лучшие практики**

1. **Частые маленькие коммиты** лучше редких больших
2. **Пишите понятные сообщения** коммитов
3. **Перед push всегда делайте pull --rebase**
4. **Не force-push в общие ветки** (main/master)
5. **Используйте .gitignore** для временных файлов
6. **Ветвление по фичам** - одна ветка = одна функциональность
7. **Регулярно мержите main в свою ветку**, чтобы не было больших конфликтов
8. **Тестируйте перед коммитом и push'ем**

Это полный набор команд Git для эффективной работы! Начните с основных команд и постепенно осваивайте продвинутые возможности.